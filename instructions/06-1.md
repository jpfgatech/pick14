# Variance Analysis

The Q network training output always has high RMSE
We wonder if this is due to the random nature of the game, or not

To do so, we create a game, and record one input state from it;

Then we randomly shuffle the opponents hand card and the top of the deck
to create different counter factual outcomes of the next 4 turns, that is required of the network to predict

To do so, we shuffle the opponents hand as well as the top 16 cards of the deck
There could be as many as 2^19 possibilities, since opponent may have 3 cards in its hand at end of player's turn
Then we simulate 4 turns, opponent - player - opponent - player
Record the raw scoring, as well as the discounted score gap

we do not mean that we should shuffle only the top 16 cards of the deck with opponent hand, keeping the bottom of the card untouched
instead, randomly pick max(16, deck remaining) + opponent hand cards out of deck remaining + opponent hand cards, shulffle the selected set, and distribute them among top of deck and opponent hand
The 'top 16' only means that the bottom of the deck does not affect the short furture horizon
But keeping top and bottom of deck separate would create a bias

We shuffle for a certain number of samples (say 128)

This counterfactual simulation should resemble the alternative option branching code we already have

We first simulate one game, one opening state, take the initial state as the output state of the virtual action of player in seat 1
and conduct a 128 shuffle simulation with display.

The point gap should be computed from the player in seat 1 perspective, check your logic against this

BTW, you tooke the greedy-stingy stragey as baseline for mistake
The correct baseline strategy should have been:
upon match, go for the combination that takes most points off the public card
(the rest of details, you should find somewhere in the static strategy code)
upon play, go for the card that minimizes point - digit. tie breaker is to play the card with highest digit
i.e. sort by (point - digit, - digit)
