# Artifacts

Training outputs (checkpoints, plots, logs) are regenerated by curriculum and PPO scripts.

**Historical note:** Runs produced before the `rl.md`-aligned implementation used a mismatched reward (global card-count delta) and a non-spec architecture. Those metrics and checkpoints are **not comparable** to current training.
